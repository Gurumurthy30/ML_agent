# Optional — only needed if the Coder sub-agent decides to use them for a given
# dataset's image_path/audio_path/free_text columns. Not installed by default because
# they're large; the Coder's system prompt is written to degrade gracefully (e.g.
# fall back to file-size-only stats) if these aren't present, so the pipeline still
# works without them on datasets that don't need them.
#
#   pip install -r requirements.txt -r requirements-modalities.txt

torch>=2.0
torchvision>=0.15
torchaudio>=2.0
transformers>=4.40
sentence-transformers>=2.6
librosa>=0.10
soundfile>=0.12
