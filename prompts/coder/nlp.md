# NLP Modality Coder Guidelines

1. Use standard NLP libraries: `transformers`, `datasets`, `torch`, `scikit-learn`.
2. Pipeline: Tokenization using pretrained AutoTokenizer, sequence classification model setup, K-fold validation, out-of-fold evaluation.
3. Save predictions to `submission.csv`.
