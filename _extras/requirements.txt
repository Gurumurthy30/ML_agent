# Core
pydantic>=2.0
PyYAML>=6.0
python-dotenv>=1.0

# Web server
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9   # multipart file uploads (POST /upload)
aiofiles>=23.0             # async file I/O in server

# Orchestration
langgraph>=0.2.28          # >=0.2.28 required for interrupt() + MemorySaver checkpointer
langchain-openai>=0.2      # ChatOpenAI wrapper — points at NVIDIA NIM, Groq, or Google
langchain-groq>=0.2
langchain-google-genai>=4.0    # 4.0+ required — consolidated google-genai SDK
langchain-nvidia-ai-endpoints>=0.3
langsmith>=0.1             # optional run monitoring — activates via LANGSMITH_TRACING=true

# Data Explorer (deterministic EDA — tabular/cv/nlp/audio)
pandas>=2.0
numpy>=1.24
scipy>=1.10
Pillow>=10.0               # image dimension/channel stats (cv modality); audio uses stdlib wave
pyarrow>=14.0              # Parquet read/write support in Session Init

# RAG (optional — degrades gracefully to fallback snippets if not installed)
chromadb>=0.5
sentence-transformers>=3.0

# Optional: Excel file support in Session Init
# openpyxl>=3.1            # uncomment to enable .xlsx upload support

# Testing
pytest>=7.0
pytest-asyncio>=0.23
